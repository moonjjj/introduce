<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="seon.je.moon.Member">
  	<insert id="member_join" parameterType="seon.je.moon.vo.Member">
  		insert into member(mb_id, mb_password,mb_name) values(#{mb_id},#{mb_password},#{mb_name});
  	</insert>
  </mapper>